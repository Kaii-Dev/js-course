// PROMISE.RESOLVE
// PROMISE.REJECT
// PROMISE.ALL

const promise = Promise.re

promise
   .then((result)=>{
      console.log('result:', result);
   })
   .catch((error)=>{
      console.log('error:', error);
   })